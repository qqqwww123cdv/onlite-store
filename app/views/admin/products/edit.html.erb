<div class="container">
  <div class="marl">
    <h1>Editing Product</h1>
    <%= render 'form', product: @product %>

    <%= link_to 'Show', admin_product_path(@product), class: "btn btn-outline-primary mt-2 mb-3" %> |
    <%= link_to 'Back', admin_products_path, class: "btn btn-outline-danger mt-2 mb-3" %>
  </div>
</div>
