<%= form_with(model: [:admin, promocode]) do |form| %>
  <% if promocode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promocode.errors.count, "error") %> prohibited this promocode from being saved:</h2>

      <ul>
        <% promocode.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code %>
  </div>

  <div class="field">
    <%= form.label :discount %>
    <%= form.number_field :discount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
