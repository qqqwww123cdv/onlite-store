<%= form_with(model:  [:admin, category], :html => {:class => "form-group"}) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mb-3">
    <%= form.label :name %>
    <%= form.text_field :name, class: "form-control max" %>
  </div>

  <div class="field mb-3">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control max" %>
  </div>

  <div class="actions">
    <%= form.button "Сomplete", type: "submit", :class=> "btn btn-primary mt-3" %>
  </div>
<% end %>
